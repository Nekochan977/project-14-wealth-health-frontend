import Logo from "../../assets/Logo.png"

const Header = () => {
    return(
        <header>
            <img className="logo" src={Logo} alt="HRnet logo"/>
        </header>
    )
}
export default Header